[profile.default]
src = "contracts"
libs = ["@openzeppelin", "@chainlink"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 10000
evm_version = "cancun"

remappings = [
    "@openzeppelin/=@openzeppelin/",
    "@chainlink/=@chainlink/"
]
